// 'use client'; add if needed for interactivity

export default function SellerPortal() {
  // Yaha login check, product upload form add karo (Supabase insert).

  return (
    <div className="container max-w-7xl mx-auto p-8">
      <div className="ornate-border ...">Seller Portal</div>
      {/* UI: Product listing, upload button, categories */}
    </div>
  );
}
